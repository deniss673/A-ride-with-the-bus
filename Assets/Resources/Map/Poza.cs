using UnityEngine;

public class Poza : MonoBehaviour
{
    
    void Start()
    {
        var go = new GameObject();
        go.AddComponent<StreetCreate>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
